# ======================================
# Podcast Transcriber 配置文件
# ======================================

# Server Configuration
PORT=3000
# HOST=0.0.0.0  # 取消注释允许外网访问

# ======================================
# Whisper 转录配置
# ======================================

USE_LOCAL_WHISPER=true

# 模型选择: tiny, base, small, medium, large-v3
# - tiny/base: 速度快，精度一般
# - large-v3: 速度慢，精度最高（推荐 GPU 使用）
WHISPER_MODEL=base

# 设备选择: cpu 或 cuda
# - cpu: 适用于 Mac 或无 GPU 的机器
# - cuda: 适用于 NVIDIA GPU（速度快 10-30 倍）
WHISPER_DEVICE=cpu

# 计算精度: int8, float16, float32
# - int8: CPU 推荐，内存占用小
# - float16: GPU 推荐，速度快精度高
WHISPER_COMPUTE_TYPE=int8

# ======================================
# OpenAI API 配置（可选，当前已禁用）
# ======================================

# 如需启用 AI 文本优化和总结功能，取消下面的注释
# OPENAI_API_KEY=your-api-key
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_MODEL_OPTIMIZATION=gpt-3.5-turbo
# OPENAI_MODEL_SUMMARY=gpt-4
