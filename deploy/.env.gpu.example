# ======================================
# GPU 服务器配置 (Ubuntu + NVIDIA GPU)
# ======================================

# Server Configuration
PORT=3000
HOST=0.0.0.0

# Whisper Configuration (GPU Mode)
USE_LOCAL_WHISPER=true
WHISPER_MODEL=large-v3
WHISPER_DEVICE=cuda
WHISPER_COMPUTE_TYPE=float16

# Model Options:
# - tiny:     最快，精度最低 (~1GB VRAM)
# - base:     快速，精度一般 (~1GB VRAM)
# - small:    中等速度和精度 (~2GB VRAM)
# - medium:   较慢，精度较高 (~5GB VRAM)
# - large-v3: 最慢，精度最高 (~10GB VRAM) - 推荐4090使用

# Compute Type Options:
# - float16:  GPU推荐，速度快精度高
# - int8:     CPU推荐，内存占用小
# - float32:  最高精度，最慢

# OpenAI API (Optional - currently disabled)
# OPENAI_API_KEY=your-api-key
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_MODEL_OPTIMIZATION=gpt-3.5-turbo
# OPENAI_MODEL_SUMMARY=gpt-4
